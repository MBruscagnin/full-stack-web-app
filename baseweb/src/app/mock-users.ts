import {User} from './User';

export const mockusers: User[] = [
    { 
        username: 'user 1', 
        mail: 'user@user.it', 
        address: 'location', 
        auctions: []
         }
    ];
